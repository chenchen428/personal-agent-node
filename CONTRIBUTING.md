# Contributing

Use Node.js 22 or newer. Keep Personal Agent Cloud optional, keep runtime state outside Git, add focused tests for behavior changes, and run `npm test` before opening a pull request. Never commit secrets or user data.
