# Self-hosted Edge

The Edge is optional. A user may deploy the included WireGuard, ACME, and Nginx transport plane to their own Linux host, or configure ngrok, Cloudflare Tunnel, or FRP through the provider contract. Never upload Node data, credentials, Agent state, or model request bodies to the Edge.
