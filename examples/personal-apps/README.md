# Personal App examples

These examples are development references for source checkouts. They are not
part of the release payload and installers must never copy them into a user's
`workspace/apps` directory.

`personal-agent.daily-brief` demonstrates the same-origin mail, Pages, shared
data, App catalog, and App history APIs with separate mobile and desktop
compositions.
