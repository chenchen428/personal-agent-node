{
  "schemaVersion": 1,
  "id": "authenticated-login",
  "workflow": "Authenticated browser login",
  "cloudIndependent": true,
  "given": "A loopback Console and tunneled authentication service configured with test-only local secrets",
  "when": "Desktop and tunneled browsers access the Console, change the access password, and attempt valid, cross-tenant, expired, and unsafe redirects",
  "acceptance": [
    "Tunneled login requires explicit local secrets while direct loopback Console access needs no login",
    "Session cookies are host-only and tenant-bound",
    "Changing the local access password invalidates existing remote sessions",
    "Unsafe return locations are rejected"
  ]
}
