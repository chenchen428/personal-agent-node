{
  "schemaVersion": 1,
  "id": "authenticated-login",
  "workflow": "Remote browser password login",
  "cloudIndependent": true,
  "given": "A remote-access authentication service configured with a test-only password verifier",
  "when": "A direct-loopback client opens the app and a remote browser attempts valid, cross-tenant, expired, and unsafe login flows",
  "acceptance": [
    "Remote and reverse-tunnel login requires an explicit password verifier",
    "Direct loopback application access does not require a password",
    "Session cookies are host-only and tenant-bound",
    "Unsafe return locations are rejected"
  ]
}
