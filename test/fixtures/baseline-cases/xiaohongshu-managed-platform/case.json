{
  "schemaVersion": 1,
  "id": "xiaohongshu-managed-platform",
  "workflow": "Social browser read connector baseline",
  "cloudIndependent": true,
  "given": "Deterministic legacy managed and current OpenCLI social providers with mocked external responses",
  "when": "Browser executor status, fixed-site opening, historical login, and read-only content inputs are exercised",
  "acceptance": [
    "Connections that depend on OpenCLI browser control are available only on Windows and macOS; Linux omits them from the catalog, performs no background probe, and rejects their API and CLI surfaces",
    "Historical account login is coordinated without leaking verification codes",
    "Read-only search results are normalized",
    "Invalid legacy login and QR input is rejected",
    "Social status separates browser readiness, visible platform login and read/search capabilities; missing or unknown login never appears connected, while a fixed official-site login handoff remains available",
    "OpenCLI execution is allowlisted, bounded, shell-free, and does not receive unrelated Personal Agent secrets",
    "Xiaohongshu and Twitter/X check login in the same browser profile before search/read, return a typed human-login blocker and resume authorized reads after confirmation without reading browser cookies or exposing OpenCLI write commands"
  ]
}
