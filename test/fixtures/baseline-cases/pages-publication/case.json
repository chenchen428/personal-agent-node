{
  "schemaVersion": 1,
  "id": "pages-publication",
  "workflow": "Pages publication and private preview",
  "cloudIndependent": true,
  "given": "Private publication metadata and deterministic self-contained page content",
  "when": "Private previews, public page rendering, and conversation fragments are produced",
  "acceptance": [
    "Private publication records remain authenticated",
    "Rendered pages do not expose internal system modules",
    "Published and private content have distinct routes",
    "Internal Page and thumbnail URLs remain relative to the current desktop or mobile origin",
    "pa-cli pages publish returns the managed-tunnel HTTPS URL alongside the canonical internalUrl, or an explicit unavailable-domain notice when no accessible domain exists",
    "Public shareUrl is separate and never replaces the managed delivery URL or internal URL",
    "Page bundle assets are uploaded through independent per-file requests, the final publish request carries only verified relative-path references instead of embedded bundle bytes, and no aggregate 20 MiB limit is applied to the Page directory",
    "Every new Page persists different desktop and mobile thumbnails and each client renders its matching thumbnail; when omitted, the CLI generates distinct device gallery previews without browser capture",
    "Generic Page publication accepts governed Page artifacts without a template selector and new Page metadata does not contain template provenance",
    "Every new Page uses a distinct 360-430 CSS px mobile composition rather than a uniformly scaled desktop canvas; ordinary text, essential diagram labels, touch targets, dense-diagram navigation, raster density, component-scoped overflow, semantic alternatives, and user-owned visual acceptance follow the generic mobile authoring contract",
    "Page publication returns a stable pageId and Page Activity must target that id so the Activity card opens Page detail",
    "Page Activity uses the stored mobile thumbnail as representative feed media without scraping Page HTML",
    "The authenticated Pages module remains a searchable result library without a template catalog, template detail route, or template selection entry point",
    "The interior-designer representative delivery is a self-contained V5 workspace whose booklet is the primary entry; six discipline-specific SVG drawings, editable semantic Three.js 3D, one-view-at-a-time panorama confirmation and a final gated krpano tour share one current geometry revision; artifact-workflow.json records independent revision, confirmation and dependency invalidation; raw evidence stays private, professional-file and internal-data downloads are absent, and visual acceptance is owned by the user"
  ]
}
