{
  "schemaVersion": 1,
  "id": "pages-publication",
  "workflow": "Pages publication and private preview",
  "cloudIndependent": true,
  "given": "Private publication metadata and deterministic self-contained page content",
  "when": "Private previews, public page rendering, and conversation fragments are produced",
  "acceptance": [
    "Private publication records remain authenticated",
    "Rendered pages do not expose internal system modules",
    "Published and private content have distinct routes",
    "Internal Page and thumbnail URLs remain relative to the current desktop or mobile origin",
    "pa-cli pages publish returns the managed-tunnel HTTPS URL alongside the canonical internalUrl, or an explicit unavailable-domain notice when no accessible domain exists",
    "Public shareUrl is separate and never replaces the managed delivery URL or internal URL",
    "Every new Page persists different desktop and mobile thumbnails and each client renders its matching thumbnail; when omitted, the CLI generates distinct device gallery previews without browser capture",
    "The Agent lists and inspects registered Page templates before generation, selects every semantic match, and passes the template contract, linked Skill, implementation version, exact generator, artifact marker, contract digest and user-owned visual acceptance contract into a child task",
    "Registered template publication fails closed before upload when the HTML marker, ID or version differs, then the Node re-verifies and persists template ID, version, contract digest, marker and exact artifact SHA-256 in public or private Page metadata",
    "Page publication returns a stable pageId and Page Activity must target that id so the Activity card opens Page detail",
    "Page Activity uses the stored mobile thumbnail as representative feed media without scraping Page HTML",
    "The authenticated Pages module exposes a compact static template catalog whose cover and trusted sandboxed desktop/mobile detail preview consume the same hash-verified generated Page artifact, reload on device changes, and render functional same-origin 3D while the detail shell appears before artifact loading completes",
    "A template detail opens that same generated Page example in a separate authenticated tab",
    "The built-in gift advisor template v1 is linked to gift-advisor, keeps ordinary gift discovery in the main conversation until recipient evidence and constraints are sufficient, separates observed facts, cautious inferences and unknowns, requires at least two gift strategies and three to nine distinct in-budget product families, requires one verified public HTTPS product page, real product image, visible listed price, availability note and matching source/check date per recommendation, generates a deterministic Page whose only remote resources are declared product images and whose filtering, sorting, rationale, shortlist, theme and print interactions remain local, performs no normal Agent browser self-acceptance, and leaves visual and interaction acceptance to the user",
    "The built-in renovation delivery template v2 is linked to interior-design, is regenerated from a native trusted-Space seed by the registered Pascal v2 implementation, records seed, user-uploaded source-plan image, Agent-uploaded revision-annotation image, ImageGen render, exact SU reference and prompt hashes, project, scene, audit and file hashes, embeds both redacted evidence images plus the accepted revision-bound concept render through ordinary image elements and a sanitized deterministic Pascal scene, keeps source and annotated floor-plan evidence inside the requirements view, orders the switch as requirements, design and render while keeping design active by default, switches design and render inside one fixed non-scrolling stage, gives floor-plan evidence and render the same bounded 100–400% low-sensitivity continuous wheel zoom, fine controls, double-click, drag and damped touch-pinch image viewer, gives render additional left and right 90-degree rotation, angle feedback and unified reset, rejects stale or altered render provenance, uses a dedicated cold-start loading state, preserves concepts, requirements, quality issues, assumptions, unknowns and professional verifications, provides floor stacked, solo and exploded modes plus visibly distinct isometric 3D and top-down orthographic views, labels and model-derived SVG failure fallback, contains no remote or loopback dependency or private identity field, supports desktop and mobile landscape interaction, performs no Agent browser self-acceptance, and leaves visual and interaction acceptance to the user"
  ]
}
