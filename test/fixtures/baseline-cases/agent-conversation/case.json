{
  "schemaVersion": 1,
  "id": "agent-conversation",
  "workflow": "Local Agent conversation",
  "cloudIndependent": true,
  "given": "An isolated local broker database and a deterministic Agent runner",
  "when": "Conversation inputs create a session and additional messages arrive while it is running",
  "acceptance": [
    "A local session stores ordered commands and events",
    "Concurrent input is serialized",
    "In-progress Worker tasks resume after a service restart, surface live recovery status, and report completion through the main Agent",
    "A Work reports verified artifact information in its final chat reply while the generic completion event remains domain-neutral, and the main Agent uses that information to author Activity",
    "Child tasks require an Agent-generated title of at most 20 characters and description of at most 100 characters, and both fields remain updateable through the Personal Agent CLI",
    "The main Agent proactively delegates substantive work, splits independent branches when useful, stays available for user-facing status and final replies, and never duplicates delegated implementation",
    "Child task execution preserves the complete latest visible parent request and all delegated prompt text, including content split by nested shell quotes",
    "Internal worker hook inputs never appear in user conversation history",
    "Execution completion prompts compare verified outputs with the full user goal, continue authorized unfinished work, complete main-Agent-only QR composition and chat attachments, and distinguish genuine blockers without repeating unchanged failures; mixed status-and-action messages reach the Agent intact",
    "Mobile task detail reads only the Space-local display-event ledger: task requirements and every subsequent user-visible Worker input are persisted once and shared with desktop history, legacy explicit user input is migrated without exposing delegated or internal prompts, the detail opens on the latest 20 visible events at the absolute bottom, pages earlier events with a task-bound opaque cursor while preserving the reading anchor, follows sanitized live updates only when pinned to the tail, and never scans or falls back to raw Codex execution history",
    "The desktop conversation merges complete desktop and WeChat main-session history chronologically and labels each user message with its source",
    "Desktop conversation background updates, optimistic sends and image layout changes preserve the reader anchor while unpinned; initial latest positioning and explicit scrolling or jumping to the bottom enable following new messages",
    "Chat image attachments and Markdown images open an accessible in-app preview; closing or Escape preserves reading position and restores focus without changing ordinary file or PDF links",
    "The desktop conversation composer clears text and attachments as soon as an optimistic user message is accepted, without waiting for the network request or Agent reply, and restores the submitted draft when delivery fails",
    "The shared desktop-shell and browser conversation composer accepts pasted clipboard images while preserving ordinary text paste, immediately uploads them into the current Space managed inbound directory, previews and removes them before sending, enforces the registered count and byte limits, and submits only governed object references with authenticated raw/view URLs",
    "Desktop conversation images render as square previews above the related message text and open the authenticated private-file preview flow",
    "Desktop and mobile conversation readers preserve structured final-reply image and file attachments with safe same-origin preview/download actions, image dimensions and alt text, file names and sizes, and pending, sent, failed, or ambiguous delivery state",
    "Canonical Task, Page and private-file records keep same-origin relative paths; task CLI results expose a complete Managed Mobile HTTPS url when available or an empty url with linkNotice when unavailable, and no caller concatenates a domain with internalUrl; drive paths, absolute filesystem paths, file URLs and loopback URLs never enter a remote reply",
    "Sites, session progress and Page URLs use one current-Space domain resolver for independently verified custom domains, inherited domains and managed domains; matching verification and live Relay facts override stale local-only/display URL metadata, unavailable states retain their actual reason, and an authenticated-route login redirect never removes a valid domain URL or requests rebinding",
    "Conversation execution does not require Cloud",
    "Runtime settings default to Codex and retain separate Codex/Claude Code profiles owned by the installation's personal Space; child Spaces read the effective main settings with readOnly/inherited/sourceSpace metadata, reject save/detect/test and legacy writes server-side, and use the main credential only in process memory on each fresh turn without copying it or activating historical child configuration; secrets never appear in API/argv/events, draft tests never save settings, engine session IDs and per-Space authorization/Token limits remain isolated",
    "Release and final acceptance install the GitHub Release asset and use authenticated /app/chat",
    "A unique prompt reaches the real Agent runtime and its Agent reply is observed in the same session",
    "wechatRequired is false and the optional connections.wechat Setup check never blocks the Agent",
    "Only the canonical main Agent can operate Activity",
    "Activity is never generated from system events",
    "Titles are limited to 30 visible characters and attachments to ten",
    "A good Activity states the outcome, meaning and next action, links a stable governed target when available, and uses only explicit managed attachments",
    "Activity attachments and final-reply attachments remain separate contracts, and a Worker can report artifact objectIds but cannot select or send reply media",
    "Search and update are owner-local and revision-safe",
    "Existing-result retrieval searches Activity before session history, never creates a child task merely to find a result, and falls back across safe local indexes before exposing internal authentication mechanics",
    "The daily Token limit defaults to 0 (unlimited), uses M units, and replies without starting the Agent when today's usage has reached a configured positive limit",
    "Every real main-Agent user turn recalls at most twelve relevant active memories from the current Space before execution, counts only actually injected records once per session-turn-memory tuple, and never gives the capability to Workers",
    "Memory stores content plus system metadata only, supports main-Agent create, revision-safe update and permanent delete through pa-cli, reactivates forgotten records on update, and exposes only authenticated read-only UI routes",
    "Memory heat combines 90-day recency and logarithmic hit frequency, active records are ordered by heat, and records become forgotten after one year without creation, update, or hit",
    "Tasks use generic Workers; retired specialist selectors fail closed without changing existing task history",
    "The release contains only thirteen cove-* skills; builtin and user sources share one authoritative UI and engine catalog, and unchanged legacy copies are excluded only by complete trusted file-tree fingerprints while all user files and backups stay untouched",
    "Codex and Claude suppress native skill autoload only inside product execution, receive the same current skill index, and refresh newly added user skills on the next turn and resume without changing global engine configuration",
    "涉及子任务时首次确认、恢复后确认、进度更新及状态回复均保留服务提供的进度页面；使用现有执行详情页，远程不可用时准确说明，不拼接或伪造地址。",
    "服务成功监听后复用既有恢复机制续接start/running主会话：保留原会话历史和线程，先核实完成与外部副作用，失败或无终态落paused，避免重复恢复且不覆盖新用户回合；服务中断与用户主动停止区分，不新增队列或检查点。",
    "我的技能支持本机同源桌面导入SKILL.md或文本资源目录、摘要绑定确认移除及撤销；内置只读，不执行导入脚本，不覆盖同名，拒绝目录逃逸/符号链接/跨Space目标，目录每新回合动态读取。"
  ]
}
