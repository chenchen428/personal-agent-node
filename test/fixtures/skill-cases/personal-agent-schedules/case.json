{
  "schemaVersion": 1,
  "skill": "personal-agent",
  "title": "创建并验证每日提醒",
  "prompt": "直接通过已注册的 schedules 能力创建每天 21:00（Asia/Shanghai）提醒吃叶酸的任务。不要创建子任务代办这次计划管理；创建后查询持久化列表，只有任务 ID、Cron、时区、内容、启用状态和下次运行均正确时才报告成功。",
  "commands": [
    "personal-agent capabilities inspect schedules --json",
    "pa-cli cron create --name \"每日叶酸提醒\" --cron \"0 21 * * *\" --timezone Asia/Shanghai --prompt \"提醒用户吃叶酸。\" --json",
    "pa-cli cron list --json"
  ],
  "artifacts": [
    { "path": "expected.json", "label": "Scheduled task contract expectation" }
  ]
}
