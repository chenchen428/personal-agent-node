# Native Pascal Agent delivery acceptance

The case passes only when `npm run interior:verify-agent-delivery` regenerates the interior-designer representative delivery through the governed project, Pascal compile, professional audit, Page generation, model-derived cover, Agent provenance, and artifact hash pipeline without byte drift.

Visual and interaction acceptance remains owned by the user.
