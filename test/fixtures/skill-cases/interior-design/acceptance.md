# Native Pascal template acceptance

The case passes only when `npm run interior:verify-template` regenerates the built-in example through the governed project, Pascal compile, professional audit, Page generation, model-derived cover, and artifact hash pipeline without byte drift.

Visual and interaction acceptance remains owned by the user.
