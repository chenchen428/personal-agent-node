# Skills

`registry/skills.json` is the authoritative skill catalog and top-level `skills/` is the portable source installed on a customer machine. Every active skill has `SKILL.md`, Agent UI metadata, and a reproducible case under `test/fixtures/skill-cases/`.
