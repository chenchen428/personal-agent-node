# Legacy channel compatibility

The user-facing product domain is now **Connections**. Read the generated [connection operations](connections.md) reference for the authoritative capability, Skill, and CLI contract.

The old `channel` and provider-specific commands remain compatibility aliases only. Do not use this file as a capability definition and do not add new providers here.
